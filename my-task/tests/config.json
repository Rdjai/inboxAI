{
  "base_commit": "d9411574d722e4bfa61252713a842c7baf0cdb53",
  "fail_to_pass": [
    "list query validation preserves accountId",
    "search query validation preserves accountId",
    "email list scopes pagination and stats to the requested mailbox",
    "search requests keep the requested mailbox filter",
    "dashboard analytics honor the requested mailbox"
  ],
  "pass_to_pass": [
    "pagination normalization still caps oversized limits",
    "ai service still classifies refund language deterministically"
  ]
}
